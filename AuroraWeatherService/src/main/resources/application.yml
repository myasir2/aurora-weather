server:
  port: 8080
  ssl:
    enabled: true
    key-alias: auroraweatherservice
    key-store: classpath:keystore.p12
    key-store-password: secret
    key-store-type: PKCS12

#grpc:
#  server:
#    port: 9090
#    security:
#      enabled: true
#      key-store-format: PKCS12
#      key-store-password: secret
#      private-key: classpath:server.key
#      certificate-chain: classpath:server.p12

spring:
  application:
    name: AuroraWeatherService

logging:
  level:
    root: INFO
